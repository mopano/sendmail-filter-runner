/**
 * Simple standalone Mail filter. The {@link com.sendmail.milter.standalone.SimpleMilterServer} class implements a
 * simple standalone filter, which you can use with your own classes that implement the
 * {@link com.sendmail.milter.IMilterHandler} interface.
 * <p>
 * Current version can only run one filter at a time.
 */
package com.sendmail.milter.standalone;
